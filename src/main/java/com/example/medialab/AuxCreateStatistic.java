package com.example.medialab;

public class AuxCreateStatistic {
    static String CreateStatistic(String word, String tries, String winner) {
        return "Word: " + word + "\t\t" + "Tries rem: " + tries + "\t\t" + "Winner: " + winner + "\n";
    }
}
